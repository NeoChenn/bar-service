SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-service-role-key   # service role key — bypasses RLS, never use in frontend
STRIPE_SECRET_KEY=sk_test_...        # test key during development
STRIPE_WEBHOOK_SECRET=whsec_...      # from: stripe listen --forward-to localhost:8000/orders/webhook
FRONTEND_URL=http://localhost:5173
